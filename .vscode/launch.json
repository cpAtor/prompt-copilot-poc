{
    "configurations": [
        {
            "type": "msedge",
            "name": "Launch index.html with Edge",
            "request": "launch",
            "file": "${workspaceFolder}/index.html"
        }
    ]
}